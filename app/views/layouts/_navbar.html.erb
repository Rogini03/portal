<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">PORTAL</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
    <% if current_user %>
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Patients",patients_path, :class=>"nav-link active" %>
        </li>
        <% if current_user.doctor? %>
        <li class="nav-item">
          <%= link_to "Graphs",graphs_path, :class=>"nav-link active" %>
        </li>
        <% end %>
      </ul>
    </div>
    <div class="d-flex justify-content-end">
      <ul class="navbar-nav">
        <li class="nav-item">
          <p style="color:white;margin-top:10px;">Logged In As: <%= current_user.name.humanize %>(<%=current_user.role.humanize %>)</p>
        </li>
        <li class="nav-item">
          <%= button_to 'Logout', destroy_user_session_path, method: :delete, :class=>"btn btn-primary", form: { turbolink: false}  %> 
        </li>
      </ul>
    </div>
    <%end %>
  </div>
</nav>